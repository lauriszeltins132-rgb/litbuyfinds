import { createSeoLandingConfigPage } from "@/lib/seo-landing-config-page";

const { generateMetadata, Page } = createSeoLandingConfigPage("oopbuy-spreadsheet");

export { generateMetadata };
export default Page;
