import { createAgentCouponLandingPage } from "@/lib/agent-coupon-landing-page";

const { generateMetadata, Page } = createAgentCouponLandingPage("hipobuy-coupons");

export { generateMetadata };
export default Page;
